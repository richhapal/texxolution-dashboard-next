# Environment Variables for RTK Query Backend Integration
# Copy this file to .env.local and fill in your actual values

# Backend API Configuration for RTK Query
NEXT_PUBLIC_BACKEND_DASHBOARD_URL=http://localhost:3001/api

# Authentication
# RTK Query uses cookies for authentication with the format: token=your-jwt-token
# The system will automatically read the token from document.cookie

# Upload Configuration (handled by backend)
# - Max file size: 10MB per file
# - Allowed types: image/jpeg, image/jpg, image/png, image/webp  
# - Max files per batch: 5 files
# - Upload paths: products, homepage, utils

# RTK Query Features:
# - Automatic caching and refetching
# - Optimistic updates
# - Error handling with retry logic
# - Background sync and invalidation
# - Real-time UI updates

# Authentication is required for all upload endpoints
# Ensure users are logged in before accessing the image upload page
